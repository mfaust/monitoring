---
version: '3.3'

networks:
  frontend:
  backend:
  database:

volumes:
  database:
    driver: local
  redis:
    driver: local
  carbon:
    driver: local
  icinga:
    driver: local

services:

  beanstalkd:
    restart: always
    image: bodsch/docker-beanstalkd:1.10
    container_name: beanstalkd
    hostname: beanstalkd
    depends_on:
      - fluentd
    links:
      - fluentd
    logging:
      driver: fluentd
      options:
        tag: docker.{{.Name}}
        labels: mq
        env: SERVICE_NAME, SERVICE_TAGS
    networks:
      - database
      - backend

  database:
    restart: always
    image: bodsch/docker-mysql
    container_name: database
    hostname: database
    environment:
      MYSQL_SYSTEM_USER: root
      MYSQL_ROOT_PASS: ${MYSQL_ROOT_PASSWORD}
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - database:/srv
    depends_on:
      - fluentd
      - elk
    links:
      - fluentd
    logging:
      driver: fluentd
      options:
        tag: docker.{{.Name}}
        env: SERVICE_NAME, SERVICE_TAGS
    networks:
      - database
      - backend

  jolokia:
    restart: always
    image: moebius-registry.coremedia.vm/docker-cm-jolokia:latest
    # build: ../../docker-cm-jolokia
    container_name: jolokia
    hostname: jolokia
    volumes:
      - /etc/localtime:/etc/localtime:ro
    depends_on:
      - fluentd
    links:
      - fluentd
    logging:
      driver: fluentd
      options:
        tag: docker.{{.Name}}
        env: SERVICE_NAME, SERVICE_TAGS
    networks:
      - backend
    extra_hosts:
      osmc.local: 192.168.122.60
      blueprint-box: 192.168.252.100

  memcached:
    restart: always
    image: memcached:1-alpine
    container_name: memcached
    hostname: memcached
    volumes:
      - /etc/localtime:/etc/localtime:ro
    command: "--listen=0.0.0.0 --memory-limit=8"
    depends_on:
      - fluentd
    links:
      - fluentd
    logging:
      driver: fluentd
      options:
        tag: docker.{{.Name}}
        env: SERVICE_NAME, SERVICE_TAGS
    command: "--listen=0.0.0.0 --memory-limit=8"
    networks:
      - database
      - backend

  redis:
    restart: always
    image: redis:4-alpine
    container_name: redis
    hostname: redis
    ports:
      - 6379:6379
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - redis:/data
    depends_on:
      - fluentd
    links:
      - fluentd
    logging:
      driver: fluentd
      options:
        tag: docker.{{.Name}}
        env: SERVICE_NAME, SERVICE_TAGS
    networks:
      - database
      - backend

  nginx:
    restart: always
    image: bodsch/docker-nginx:1.12.2-r1
    container_name: nginx
    hostname: nginx
    ports:
      - 80:80
      - 443:443
    depends_on:
      - fluentd
      - elk
      - grafana
      - icingaweb2
      - cm-rest-service
      - documentation
    links:
      - fluentd
      - elk
      - grafana
      - icingaweb2
      - cm-icinga2-master:icinga2-master
      - cm-rest-service
      - documentation
    volumes:
      - /etc/localtime:/etc/localtime:ro
      # - ../../docker-cm-data/rootfs/data/www/entry-page:/var/www/entry-page
      - ../config/nginx/etc/nginx/sites-enabled/01-all-in-one.conf:/etc/nginx/sites-enabled/01-all-in-one.conf:ro
      - ../config/nginx/etc/nginx/nginx.conf-aio:/etc/nginx/nginx.conf:ro
      - ./ssl:/etc/nginx/secure/localhost:ro
    logging:
      driver: fluentd
      options:
        tag: docker.{{.Name}}
        env: SERVICE_NAME, SERVICE_TAGS
    networks:
      - frontend
      - backend

  carbon:
    restart: always
    image: bodsch/docker-go-carbon:0.12.0
    container_name: carbon
    hostname: carbon
    ports:
      - 2003:2003
      - 2003:2003/udp
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - carbon:/srv
    depends_on:
      - fluentd
    links:
      - fluentd
    logging:
      driver: fluentd
      options:
        tag: docker.{{.Name}}
        env: SERVICE_NAME, SERVICE_TAGS
    networks:
      - backend

  graphite:
    restart: always
    image: bodsch/docker-graphite:1.1.3
    container_name: graphite
    hostname: graphite
    environment:
      # THIS TURN OFF THE INTERNAL CARBON!
      # USE THIS ONLY WITH AN OTHER CARBON-WRITER (e.g. bodsch/docker-go-carbon)
      USE_EXTERNAL_CARBON: 'true'
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - carbon:/srv
    depends_on:
      - fluentd
    links:
      - fluentd
    logging:
      driver: fluentd
      options:
        tag: docker.{{.Name}}
        env: SERVICE_NAME, SERVICE_TAGS
    networks:
      - backend

  grafana:
    restart: always
    image: bodsch/docker-grafana:5.1.3
    container_name: grafana
    hostname: grafana
    environment:
      URL_PATH: /grafana/
      ORGANISATION: CoreMedia
      DATABASE_TYPE: mysql
      MYSQL_HOST: database
      MYSQL_ROOT_PASS: ${MYSQL_ROOT_PASSWORD}
      GRAPHITE_HOST: graphite
      # WE USE AN SEPERATE CARBON SERVICE
      # IF NOT, THEN SET THEN TO $GRAPHITE_HOST
      # (see above in this configuration part!)
      CARBON_HOST: carbon
      MEMCACHE_HOST: memcached
      # LDAP_SERVER: ${LDAP_SERVER}
      # LDAP_PORT: 389
      # LDAP_BIND_DN: ${LDAP_BIND_DN}
      # LDAP_BIND_PASSWORD: ${LDAP_BIND_PASSWORD}
      # LDAP_BASE_DN: ${LDAP_BASE_DN}
      # LDAP_GROUP_DN: ${LDAP_GROUP_DN}
      # LDAP_SEARCH_FILTER: ${LDAP_SEARCH_FILTER}
    depends_on:
      - fluentd
      - database
      - memcached
    links:
      - fluentd
      - database
      - memcached
    volumes:
      - /etc/localtime:/etc/localtime:ro
      # - /share/opt/grafana/dashboards:/opt/grafana/dashboards
    logging:
      driver: fluentd
      options:
        tag: docker.{{.Name}}
        env: SERVICE_NAME, SERVICE_TAGS
    networks:
      - frontend
      - backend
      - database

  cm-icinga2-master:
    restart: always
    image: moebius-registry.coremedia.vm/docker-cm-icinga2:1806
    # build: ../../docker-cm-icinga2
    container_name: cm-icinga2-master
    hostname: cm-icinga2-master
    depends_on:
      - fluentd
      - database
      - redis
    links:
      - fluentd
      - database
      - redis
    environment:
      ICINGA2_MASTER: cm-icinga2-master
      ICINGA2_API_USERS: root:icinga,icingaweb:icingaweb,dashing:dashing,cert:foo-bar
      BASIC_AUTH_USER: ${CERT_SERVICE_BA_USER}
      BASIC_AUTH_PASS: ${CERT_SERVICE_BA_PASSWORD}
#      ICINGA_CERT_SERVICE_BA_USER: admin
#      ICINGA_CERT_SERVICE_BA_PASSWORD: admin
      ICINGA_CERT_SERVICE_API_USER: root
      ICINGA_CERT_SERVICE_API_PASSWORD: icinga
      ICINGA_CERT_SERVICE_SERVER: icinga2-master
      ICINGA_CERT_SERVICE_PORT: 8080
      ICINGA_CERT_SERVICE_PATH: /
      MYSQL_HOST: database
      MYSQL_ROOT_PASS: ${MYSQL_ROOT_PASSWORD}
      IDO_PASSWORD: ${IDO_PASSWORD}
      IDO_DATABASE_NAME: icinga2core
      CARBON_HOST: carbon
      REDIS_HOST: redis
    ports:
      - 5665:5665
      - 8080
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - icinga:/var/lib/icinga2
    logging:
      driver: fluentd
      options:
        tag: docker.{{.Name}}
        env: SERVICE_NAME, SERVICE_TAGS
    networks:
      - backend
      - database
    extra_hosts:
      osmc.local: 192.168.122.60
      blueprint-box: 192.168.252.100

  icingaweb2:
    restart: always
    image: bodsch/docker-icingaweb2:2.5.3
    container_name: icingaweb2
    hostname: icingaweb2
    environment:
      MYSQL_HOST: database
      MYSQL_ROOT_PASS: ${MYSQL_ROOT_PASSWORD}
      IDO_PASSWORD: ${IDO_PASSWORD}
      IDO_DATABASE_NAME: icinga2core
      ICINGA2_HOST: cm-icinga2-master
      ICINGA2_MASTER: cm-icinga2-master
      ICINGA2_CMD_API_USER: icingaweb
      ICINGA2_CMD_API_PASS: icingaweb
      GRAPHITE_HOST: graphite
      ICINGAWEB2_USERS: coremedia:coremedia,icinga:icinga
      # LDAP_AD: 'true'
      # LDAP_SERVER: ${LDAP_SERVER}
      # LDAP_PORT: 389
      # LDAP_BIND_DN: ${LDAP_BIND_DN}
      # LDAP_BIND_PASSWORD: ${LDAP_BIND_PASSWORD}
      # LDAP_BASE_DN: ${LDAP_BASE_DN}
      # LDAP_FILTER: ${LDAP_FILTER}
      # LDAP_ROLE_GROUPS: cm-common
    depends_on:
      - fluentd
      - database
      - cm-icinga2-master
    links:
      - fluentd
      - database
      - cm-icinga2-master:cm-icinga2-master
    volumes:
      - /etc/localtime:/etc/localtime:ro
    logging:
      driver: fluentd
      options:
        tag: docker.{{.Name}}
        env: SERVICE_NAME, SERVICE_TAGS
    networks:
      - frontend
      - backend
      - database

  documentation:
    restart: always
    image: moebius-registry.coremedia.vm/docker-documentation:1806
    # build: ../../docker-documentation
    container_name: documentation
    hostname: documentation
    ports:
      - 8080
    volumes:
      - /etc/localtime:/etc/localtime:ro
    depends_on:
      - fluentd
    links:
      - fluentd
    logging:
      driver: fluentd
      options:
        tag: docker.{{.Name}}
        env: SERVICE_NAME, SERVICE_TAGS
    networks:
      - frontend
      - backend

  cm-dashing:
    restart: always
    image: moebius-registry.coremedia.vm/docker-cm-dashing:1806
    # build: ../../docker-cm-dashing
    container_name: cm-dashing
    hostname: cm-dashing
    environment:
      ICINGA_MASTER: cm-icinga2-master
      ICINGA_CERT_SERVICE_BA_USER: ${CERT_SERVICE_BA_USER}
      ICINGA_CERT_SERVICE_BA_PASSWORD: ${CERT_SERVICE_BA_PASSWORD}
      ICINGA_CERT_SERVICE_API_USER: dashing
      ICINGA_CERT_SERVICE_API_PASSWORD: dashing
      ICINGA_CERT_SERVICE_SERVER: nginx
      ICINGA_CERT_SERVICE_PORT: 80
      ICINGA_CERT_SERVICE_PATH: /cert-service
    depends_on:
      - fluentd
      - cm-icinga2-master
      - nginx
    links:
      - fluentd
      - cm-icinga2-master
      - nginx
    volumes:
      - /etc/localtime:/etc/localtime:ro
    logging:
      driver: fluentd
      options:
        tag: docker.{{.Name}}
        env: SERVICE_NAME, SERVICE_TAGS
    networks:
      - frontend
      - backend


  # CoreMedia Glue and Logic
  #
  cm-rest-service:
    restart: always
    image: moebius-registry.coremedia.vm/docker-cm-rest-service:1806
    # build: ../../docker-cm-rest-service
    container_name: cm-rest-service
    hostname: cm-rest-service
    environment:
      LOG_LEVEL: info
      MYSQL_ROOT_PASS: ${MYSQL_ROOT_PASSWORD}
    depends_on:
      - fluentd
      - redis
      - database
    links:
      - fluentd
      - redis
      - database
    volumes:
      - /etc/localtime:/etc/localtime:ro
    logging:
      driver: fluentd
      options:
        tag: docker.{{.Name}}
        env: SERVICE_NAME, SERVICE_TAGS
    networks:
      - frontend
      - backend
      - database
    extra_hosts:
      osmc.local: 192.168.122.60
      blueprint-box: 192.168.252.100

  # single service
  # use MQ to process data
  cm-service-discovery:
    restart: always
    image: moebius-registry.coremedia.vm/docker-cm-service-discovery:1806
    # build: ../../docker-cm-service-discovery
    container_name: cm-service-discover
    hostname: cm-service-discover
    environment:
      LOG_LEVEL: info
      REFRESH_ENABLED: 'true'
    depends_on:
      - fluentd
      - jolokia
      - beanstalkd
      - redis
      - database
    links:
      - fluentd
      - jolokia
      - beanstalkd
      - redis
    volumes:
      - /etc/localtime:/etc/localtime:ro
    logging:
      driver: fluentd
      options:
        tag: docker.{{.Name}}
        env: SERVICE_NAME, SERVICE_TAGS
    networks:
      - backend
      - database
    extra_hosts:
      osmc.local: 192.168.122.60
      blueprint-box: 192.168.252.100

  # single service
  # use MQ to process data
  cm-data-collector:
    restart: always
    image: moebius-registry.coremedia.vm/docker-cm-data-collector:1806
    # build: ../../docker-cm-data-collector
    container_name: cm-data-collector
    hostname: cm-data-collector
    environment:
      LOG_LEVEL: info
    depends_on:
      - fluentd
      - jolokia
      - beanstalkd
      - redis
      - database
    links:
      - fluentd
      - jolokia
      - beanstalkd
      - redis
    volumes:
      - /etc/localtime:/etc/localtime:ro
    logging:
      driver: fluentd
      options:
        tag: docker.{{.Name}}
        env: SERVICE_NAME, SERVICE_TAGS
    networks:
      - backend
      - database
    extra_hosts:
      osmc.local: 192.168.122.60
      blueprint-box: 192.168.252.100

  # single service
  # use MQ to process data
  cm-carbon-client:
    restart: always
    image: moebius-registry.coremedia.vm/docker-cm-carbon-client:1806
    # build: ../../docker-cm-carbon-client
    container_name: cm-carbon-client
    hostname: cm-carbon-client
    environment:
      LOG_LEVEL: info
      # USE ONLY WITH AND EXTERNAL CARBON-WRITER!
      GRAPHITE_HOST: carbon
    depends_on:
      - fluentd
      - redis
    links:
      - fluentd
      - redis
    volumes:
      - /etc/localtime:/etc/localtime:ro
    logging:
      driver: fluentd
      options:
        tag: docker.{{.Name}}
        env: SERVICE_NAME, SERVICE_TAGS
    networks:
      - backend
      - database

  # single service
  # use MQ to process data
  cm-grafana-client:
    restart: always
    image: moebius-registry.coremedia.vm/docker-cm-grafana-client:1806
    # build: ../../docker-cm-grafana-client
    container_name: cm-grafana-client
    hostname: cm-grafana-client
    environment:
      LOG_LEVEL: debug
      GRAFANA_HOST: nginx
    depends_on:
      - fluentd
      - beanstalkd
      - redis
      - database
    links:
      - fluentd
      - nginx
      - beanstalkd
      - redis
      - database
    volumes:
      - /etc/localtime:/etc/localtime:ro
    logging:
      driver: fluentd
      options:
        tag: docker.{{.Name}}
        env: SERVICE_NAME, SERVICE_TAGS
    networks:
      - frontend
      - backend
      - database

  # single service
  # use MQ to process data
  cm-icinga-client:
    restart: always
    image: moebius-registry.coremedia.vm/docker-cm-icinga-client:1806
    # build: ../../docker-cm-icinga-client
    container_name: cm-icinga-client
    hostname: cm-icinga-client
    environment:
      LOG_LEVEL: info
      ICINGA_MASTER: cm-icinga2-master
      ICINGA_CERT_SERVICE_BA_USER: ${CERT_SERVICE_BA_USER}
      ICINGA_CERT_SERVICE_BA_PASSWORD: ${CERT_SERVICE_BA_PASSWORD}
      ICINGA_CERT_SERVICE_API_USER: dashing
      ICINGA_CERT_SERVICE_API_PASSWORD: dashing
      ICINGA_CERT_SERVICE_SERVER: nginx
      ICINGA_CERT_SERVICE_PORT: 80
      ICINGA_CERT_SERVICE_PATH: /cert-service
      # ENABLE_NOTIFICATIONS: 'true'
    depends_on:
      - fluentd
      - beanstalkd
    links:
      - fluentd
      - beanstalkd
      - nginx
    volumes:
      - /etc/localtime:/etc/localtime:ro
      # - ../../docker-cm-data/rootfs/data/etc/icinga_server_config.yml:/etc/icinga_server_config.yml
    logging:
      driver: fluentd
      options:
        tag: docker.{{.Name}}
        env: SERVICE_NAME, SERVICE_TAGS
    networks:
      - frontend
      - backend
      - database
    extra_hosts:
      osmc.local: 192.168.122.60
      blueprint-box: 192.168.252.100

  elk:
    restart: always
    image: sebp/elk:latest
    container_name: elk
    ports:
    - 5044:5044
    - 5601:5601
    - 9200:9200
    networks:
      - backend
    volumes:
    - es-data:/var/lib/elasticsearch

  fluentd:
    restart: always
    hostname: fluentd
    # build: ../../docker-cm-fluentd
    image: moebius-registry.coremedia.vm/docker-cm-fluentd
    container_name: fluentd
    depends_on:
      - elk
    links:
      - elk
    networks:
      - backend
    ports:
    - 24224:24224/tcp
    - 24224:24224/udp
